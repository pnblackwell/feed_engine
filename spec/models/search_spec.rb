require 'spec_helper'

describe Search do
  describe '#generate_feed_items' do
    it 'creates new feed items specific to the search and feed' do
      pending 'is this test valuable, since its being hit from feed?'
    end
  end
end
