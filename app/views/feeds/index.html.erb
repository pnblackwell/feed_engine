<div class="content">
  <% @feeds.each do |feed| %>
   <%= feed.name %>
     <% feed_items = feed.feed_items %>
      <p class="thumbnails">
    <% feed_items.each do |feed_item| %>
      <%= image_tag(@image) %>
    <% end %> 
      </p>
  <% end %> 
</div>

