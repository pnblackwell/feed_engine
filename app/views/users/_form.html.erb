<%= form_for @user do |f| %>

      <%= f.label :email %>
      <%= f.text_field :email %>
      <%= f.hidden_field :username %>

      <%= f.submit "Submit", class:'btn btn-primary' %>
    <% end %>
